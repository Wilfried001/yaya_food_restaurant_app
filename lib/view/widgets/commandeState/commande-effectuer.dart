import 'package:flutter/material.dart';

class CommandeEffectuer extends StatelessWidget {
  const CommandeEffectuer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
