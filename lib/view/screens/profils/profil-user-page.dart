import 'package:flutter/material.dart';

class ProfilUser extends StatelessWidget {
  const ProfilUser({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Profil User'),
    );
  }
}
