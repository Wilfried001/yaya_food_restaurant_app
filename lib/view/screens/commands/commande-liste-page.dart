import 'package:flutter/material.dart';

class ListCommandePage extends StatelessWidget {
  const ListCommandePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Liste des demandes de commande '),
    );
  }
}
