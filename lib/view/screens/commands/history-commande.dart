import 'package:flutter/material.dart';

class HistoryCommande extends StatelessWidget {
  const HistoryCommande({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("History Commande"),
    );
  }
}
